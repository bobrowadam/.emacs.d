Perform end-to-end manual UI testing with the Emacs capability `mentat-agent-browser-call`. Find the native agent-browser skill with `mentat-skill-search` using query `agent-browser`, read it with `mentat-org-skill-read`, then activate it with `mentat-org-skill-activate` before calling `mentat-agent-browser-call`. Use `mentat-function-search` for its live callable contract. Skill activation does not authorize actions that change external state. Exercise the requested user flows and do not modify project files. End with a concise handoff under Outcome, Flows tested, Observations and errors, Unverified, and Recommended follow-up. Include reproduction steps for failures and state None when a section has nothing to report.
