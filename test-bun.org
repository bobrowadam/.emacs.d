#+TITLE: Bun and TypeScript Babel Test

* TypeScript Test
#+begin_src typescript
interface Person {
  name: string;
  age: number;
}

const bob: Person = {
  name: "Bob",
  age: 42
};

console.log(`Name: ${bob.name}, Age: ${bob.age}`);
#+end_src

* Bun JavaScript Test
#+begin_src bun
const message: string = "Hello, Bun JavaScript!";
console.log(message);

// Some additional TypeScript-like syntax to test highlighting
const numbers: number[] = [1, 2, 3];
const doubled = numbers.map((x: number) => x * 2);
console.log(doubled);
#+end_src

* Bun TypeScript Test
#+begin_src ts
interface Greeting {
  message: string;
}

const greeting: Greeting = {
  message: "Hello, TypeScript with Bun!"
};

console.log(greeting.message);
#+end_src